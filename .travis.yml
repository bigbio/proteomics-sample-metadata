os: linux
language: python
install:
  - pip install sdrfcheck

script:
  - echo `git diff-tree --no-commit-id --name-only -r ${TRAVIS_COMMIT}`
  - echo `git diff-tree --no-commit-id --name-only -r ${TRAVIS_COMMIT}` | grep "sdrf"